---
description: Readiness scorecard for a framework — in scope or not yet adopted.
---
**Before any data read: call `Drata_listWorkspaces()` first. If more than one workspace exists, ask which workspace and ask nothing else in that message; once chosen, sweep it for in-scope frameworks — if exactly one is in scope, select it silently and run (never present a one-option picker or ask to confirm it); if two or more, ask which one. Never jump straight to a framework on a multi-workspace account.** Load `${CLAUDE_PLUGIN_ROOT}/skills/drata-framework-report/SKILL.md` and follow it in full — do not summarize or shortcut it. Render branded by default: resolve the header first with `Drata_getCompany` — inline the company logo as a base64 data URI only where the host can truly download and verify the image bytes, else fall back to the company name as text; never a remote image URL, never base64 typed from memory — then read `${CLAUDE_PLUGIN_ROOT}/shared/drata-brand-kit.md` and emit the deliverable as a self-contained HTML document using the §3 `.drata` theme — render it via the host artifact tool if one exists, otherwise write the HTML to a `.html` file and send that file, named `<skill-name>-<scope>-<YYYY-MM-DD>.html` using the skill's exact folder name. **Never emit the report as chat markdown; there is no markdown fallback.** Never ask the user to pick an output mode. Use unbranded plain text only if the user asked for text-only this session or the plugin's `output_mode` setting is `plain` (see `${CLAUDE_PLUGIN_ROOT}/shared/output-mode.md`). Apply the accuracy-and-sources protocol the skill references. If the user supplied arguments, treat them as scope/context: $ARGUMENTS
